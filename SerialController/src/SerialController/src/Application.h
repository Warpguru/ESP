#pragma once

/**
 * App.h - SerialController application entry points.
 *
 * Both SerialController.ino (Arduino IDE) and SerialController.cpp (PlatformIO)
 * are thin stubs that delegate setup() and loop() to these two functions.
 */

void applicationSetup();
void applicationLoop();
